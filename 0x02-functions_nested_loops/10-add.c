#include "main.h"
#include <stdio.h>
/**
 * add - A function function that
 * adds two num.
 * @one: is a number
 * @two: is a number
 * Return: lastn
 */
int add(int one, int two)
{
	int sum = one + two;

	return (sum);
}
