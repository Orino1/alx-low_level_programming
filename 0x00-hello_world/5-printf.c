#include <stdio.h>
/**
 * main -  Function main will print a string of char
 * printf is function that print a string of char
 * Return: a value of 0 which mean the program run successfully
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
