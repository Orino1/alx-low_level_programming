#include "3-calc.h"

/**
 * op_add - function that adds two numbers
 * @a: number.
 * @b: number
 * Return: a + b
 */
int op_add(int a, int b)
{
	return (a + b);
}

/**
 * op_sub - function sub a num from a nother
 * @a: number.
 * @b: number
 * Return: a - b
 */
int op_sub(int a, int b)
{
	return (a - b);
}

/**
 * op_mul - function that multp two numbesrs
 * @a: number.
 * @b: number
 * Return: a * b
 */
int op_mul(int a, int b)
{
	return (a * b);
}

/**
 * op_div - function sub a num from a nother
 * @a: number.
 * @b: number
 * Return: a / b
 */
int op_div(int a, int b)
{
	return (a / b);
}

/**
 * op_mod - function sub a num from a nother
 * @a: number.
 * @b: number
 * Return: a % b
 */
int op_mod(int a, int b)
{
	return (a % b);
}
