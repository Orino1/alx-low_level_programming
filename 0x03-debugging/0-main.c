#include "main.h"
#include <stdio.h>
/**
 * main - A function function that
 * call another function
 * Return: always (0)
 */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
